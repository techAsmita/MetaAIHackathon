[project]
name = "meta-support-env"
version = "0.1.0"
dependencies = [
    "openenv>=0.2.0",
    "openenv-core>=0.2.0",
    "fastapi",
    "uvicorn"
]

[project.scripts]
server = "main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
